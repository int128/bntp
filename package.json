{
  "name": "new-tab-bookmarks",
  "version": "0.1.0",
  "private": true,
  "description": "an extension to add the tab with bookmarks",
  "license": "Apache-2.0",
  "devDependencies": {
    "archiver": "^1.3.0",
    "react-scripts": "0.9.0"
  },
  "dependencies": {
    "immutable": "^3.8.1",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-redux": "^5.0.3",
    "redux": "^3.6.0",
    "redux-logger": "^2.10.2",
    "redux-thunk": "^2.2.0"
  },
  "scripts": {
    "start": "node scripts/start.js",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "zip": "node scripts/zip.js",
    "clean": "rm -fr build"
  }
}
